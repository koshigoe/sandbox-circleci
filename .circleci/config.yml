version: 2

jobs:
  build:
    docker:
      - image: circleci/golang
    steps:
      - checkout
      - run:
          name: Echo
          command: echo OK

workflows:
  version: 2
  build:
    jobs:
      - build
